<% content_for :style do %>
	<style>
    * {
      -webkit-box-sizing:border-box;
      -moz-box-sizing:border-box;
      box-sizing:border-box;
    }
    .btn {
      cursor:pointer;
      padding:10px 15px;
      margin:10px 10px 0 3px;
    }
    .col {float:left;}
    .cell {
      float:left;
      width:20px;
      height:20px;
      cursor:pointer;
      -webkit-transition:background 0.25s ease;
      -moz-transition:background 0.25s ease;
      transition:background 0.25s ease;
      -webkit-border-radius:3px;
      -moz-border-radius:3px;
      border-radius:3px;
    }
    .clearfix:after {
       content: " "; /* Older browser do not support empty content */
       visibility: hidden;
       display: block;
       height: 0;
       clear: both;
    }
    .input-container {margin-right:10px;}
    .game-control {margin-bottom:20px;}
    .game-board {}
    .game-score {
      margin:20px 0 0 3px;
      font-weight:bold;
      color:#1f5d8e;
    }
    label {
      display:block;
      font-size:13px;
      padding-left:5px;
    }
	</style>
<% end %>	
<h1 class="post-title">Game Of Life</h1>

<a class='link-external' href="http://en.wikipedia.org/wiki/Conway's_Game_of_Life">Game of Life Explanation on Wikipedia</a>
<p>
  Rules:<br>
  if neighbors number = 0 or 1 cell dies<br>
  if neighbors number = 2 or 3 cell lives and random reproduce one cell in nearby dead cell or empty place<br>
  if neighbors number = 4, 5, 6, 7, 8 cell dies<br>
  if previous died cell has 3 or 4 surrounding neighbor, it revives.<br>
</p>

<div id='life'></div>
<% content_for :scripts do %>
    <%= javascript_include_tag 'life' %>
    <script>
      var control = Life.init({
        container_id : '#life',
        board_width : 20,
        board_height : 20
      });
    </script>
<% end %>

<div class="post-end">(end)</div>